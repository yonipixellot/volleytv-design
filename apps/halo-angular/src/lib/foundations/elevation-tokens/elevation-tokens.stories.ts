import type { Meta, StoryObj } from '@storybook/angular';
import { ElevationTokens } from './elevation-tokens';

const meta: Meta<ElevationTokens> = {
  title: 'Tokens/Elevation',
  component: ElevationTokens,
};
export default meta;
export const Ladder: StoryObj<ElevationTokens> = {};
